---
GENERATOR_VERSION: '1.0'
ANSIBLE_METADATA: |
    {
        'metadata_version': '1.1',
        'status': ['preview'],
        'supported_by': 'community',
        'license': 'Apache 2.0'
    }
NETWORK_OS: sonic
RESOURCE: ptp_port_ds
COPYRIGHT: Copyright 2025 Dell Inc. or its subsidiaries. All Rights Reserved
DOCUMENTATION: |
    module: sonic_ptp_port_ds
    version_added: '3.1.0'
    short_description: Manage port specific PTP configurations on SONiC
    description:
      - This module provides configuration management of port-specific
        PTP parameters for devices running SONiC.
    author: 'Vidya Chidambaram (@vidyac86)'
    options:
      config:
        description:
          - Specifies port-specific PTP configurations.
        type: list
        elements: dict
        suboptions:
          interface:
            description:
              - Reference to the configured underlying interface that is used by this PTP port.
            required: true
            type: str
          role:
            description:
              - Configures the role of interface.
              - The range is from 1 to 4294967295.
            type: str
            choices: ['DYNAMIC', 'MASTER', 'SLAVE']
          localpriority:
            description:
              - Configure the local-priority attribute used for the profile G8275-1 and G8275-2.
              - The range is from 1 to 255.
            type: int
            default: 
          unicasttable:
            description:
              - The comma-separated list of ip addresses to use for master.
            type: list
      state:
        description:
          - The state of the configuration after module completion.
          - C(merged) - Merges provided interface-specific PTP configuration with on-device configuration.
          - C(replaced) - Replaces on-device PTP configuration of the specified interfaces with provided configuration.
          - C(overridden) - Overrides all on-device interface-specific PTP configurations with the provided configuration.
          - C(deleted) - Deletes on-device interface-specific PTP configuration.
        type: str
        choices:
          - merged
          - deleted
          - replaced
          - overridden
        default: merged
EXAMPLES:
  - deleted_example_01.txt
  - deleted_example_02.txt
  - merged_example_01.txt
  - replaced_example_01.txt
  - overridden_example_01.txt
